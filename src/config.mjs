export {config};

const config = {
    timeout: 3000,
    coinList: [],
    accounts: [{
      name: 'name',
      key: 'key',
      secret: 'secret'}
    ],
    telegram: {
        key: 'key',
        channel: 'channel'
    }
};

